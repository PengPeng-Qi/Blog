---
sidebar: auto
---
# 选择排序
每次从未排序区间中找到最小的元素，将其放到已排序区间的末尾。  

## 算法实现
@[code](./选择排序.js)
## 时间复杂度
最好：O(n<sup>2</sup>)  
最坏：O(n<sup>2</sup>)  
平均：O(n<sup>2</sup>)  

## 稳定性
末尾的元素和找到的元素互换顺序，故**不是稳定的排序算法**。