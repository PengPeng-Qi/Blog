---
sidebar: auto
---
# 解构赋值
解构赋值可以是任何可迭代对象
```js
let [a, b, c] = "abc";                       // ["a", "b", "c"]
let [one, two, three] = new Set([1, 2, 3]);  // [1, 2, 3]
```
## 对象解构
解构并重命名
```js {7}
let options = {
  title: "Menu",
  width: 100,
  height: 200
};

let {width: w, height: h, title} = options;

// width -> w
// height -> h
// title -> title

alert(title);  // Menu
alert(w);      // 100
alert(h);      // 200
```
将冒号与等号结合起来：
```js
let options = {
  title: "Menu"
};

let {width: w = 100, height: h = 200, title} = options;

alert(title);  // Menu
alert(w);      // 100
alert(h);      // 200
```
## 剩余模式
```js
let options = {
  title: "Menu",
  height: 200,
  width: 100
};

// title = 名为 title 的属性
// rest = 存有剩余属性的对象
let {title, ...rest} = options;

// 现在 title="Menu", rest={height: 200, width: 100}
alert(rest.height);  // 200
alert(rest.width);   // 100
```
## 不使用Let
在上面的示例中，变量都是在赋值中通过正确的方式声明的：`let {...} = {...}`。当然，我们也可以使用已有的变量，但是有一个陷阱。
```js {4}
let title, width, height;

// 这一行发生了错误，因为Js把主代码流的{...} 作为一个代码块
{title, width, height} = {title: "Menu", width: 200, height: 100};
```
我们使用括号包起来
```js {4}
let title, width, height;

// 现在就可以了
({title, width, height} = {title: "Menu", width: 200, height: 100});
alert( title ); // Menu
```
## 嵌套解构
```js
let options = {
  size: {
    width: 100,
    height: 200
  },
  items: ["Cake", "Donut"],
  extra: true
};

// 为了清晰起见，解构赋值语句被写成多行的形式
let {
  size: { // 把 size 赋值到这里
    width,
    height
  },
  items: [item1, item2], // 把 items 赋值到这里
  title = "Menu" // 在对象中不存在（使用默认值）
} = options;

alert(title);  // Menu
alert(width);  // 100
alert(height); // 200
alert(item1);  // Cake
alert(item2);  // Donut
```
> `size` 和`item` 没有对应的变量，因为我们取得是他们的内容。  
  
## 智能函数参数
