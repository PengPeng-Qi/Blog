---
sidebar: auto
---
# Web多媒体
## 图像基本概念
**图像分辨率**：确定组成一副图像的像素数据，指在水平和垂直方向上图像所具有的像素个数。  
  
**图像深度**：存储每个像素所需要的比特数。图像深度决定了图像的每个像素可能的颜色数，或可能的灰度级数。  
## 视频基本概念
**分辨率**：每一帧图像的分辨率。  
  
**帧率**：视频单位时间内包含的**视频帧**的数量。  
  
**码率**：视频单位时间内传输的数据量，一般使用`kbps`来表示。既千位每秒。  
  
**编码格式 视频帧：I帧、P帧、B帧**  
- I帧：解码依照第一帧
- P帧：解码依照前一帧
- B帧：双向依赖编码帧（既依赖前一帧、也依赖后一帧）
  
**GOP**：两个I帧之间的间隔  
  
**为什么需要编码**：
分辨率：`1920 * 1080 * 24(图像深度) / 8` = 6220800Byte (5.9M)  
  
不编码的资源会存在**空间冗余、时间冗余、编码冗余、视觉冗余**，所以体积会很大，所以需要进行**编码缩小体积**。  
  
**编码数据处理流程**：`预测(去除空间、时间冗余) -> 变换(去除空间冗余) -> 量化(去除视觉冗余) -> 熵编码(去除编码冗余)`  
  
**封装格式**：存储音视频、图像或字幕信息的一种容器。例如：`AVI、WMV、MP4...`  
  
**多媒体元素**：`video、audio、mse`  
  
**`audio、video` 元素的缺陷：**
- 不支持直接播放hls、flv等格式
- 视频资源的请求和加载无法通过代码控制
  - 分段加载
  - 清晰度无缝切换
  - 精确预加载

**媒体源扩展API：`MSE`** [Media Source Extensions](https://developer.mozilla.org/zh-CN/docs/Web/API/Media_Source_Extensions_API)  
- 无插件在web端播放流媒体
- 支持播放hls、flv、mp4等格式视频
- 可实现视频分段加载、清晰度无缝切换、自适应码率、精确预加载等
  
**流媒体协议：** `HLS`：Http Live Streaming，是一个由Apple公司提出的基于HTTP 的媒体流传输协议，用于实时音视频流的传输。**广泛应用于视频点播与直播领域**。  
  
## 总结
`flash -> HTML5 video -> MSE`  
  
新技术标准：
- `Webassembly`
- `WebCodecs`
- `WebGPU`
- `WebVR、WebXR`